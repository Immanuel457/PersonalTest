services:
  - type: web
    name: WDN-Backend
    runtime: python
    plan: free
    buildCommand: "pip install uv & uv sync"
    startCommand: "uv run uvicorn main:app --host 0.0.0.0 --port $PORT"
    
